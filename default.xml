<?xml version="1.0" encoding="UTF-8"?>
<!--
  Pamir RK3576 Linux BSP — default manifest.
  This repository hosts a single BSP manifest; `repo init` selects it
  automatically, so no `-m` flag is required.
-->
<manifest>
  <include name="rk3576-debian-ab.xml"/>
</manifest>
